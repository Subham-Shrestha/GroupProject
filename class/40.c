//wap to take input for rows and columns and take input for elements and find their sum

#include<stdio.h>
int main()
{
    int i,j,sum;
    int rows, cols;
    printf("enter rows and columns:");
    scanf("%d %d ", &rows, &cols);
    
    for(i=0; i<rows; i++)
    int arr[i],[j];

    {
        for(j=0;j<cols; j++)
        {
            sum= arr[i]+arr[j];
            printf("%d ",sum);
        }
    }

    

return 0;
}

